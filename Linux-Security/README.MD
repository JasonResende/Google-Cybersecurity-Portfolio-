# Linux System Hardening & Log Analysis
This folder showcases my proficiency with the Linux Command Line Interface (CLI) to manage system security.

### Projects included:
1.  **Permission Auditing:** Using `chmod` and `chown` to enforce the Principle of Least Privilege.
2.  **Log Analysis:** Using `grep` and `awk` to parse `/var/log/auth.log` for signs of brute-force attacks.
* **Key Skills:** CLI Navigation, File Permissions, Grep/Regex, Log Triage.

[Back to Main Portfolio](../README.md)
